name: Deploy

on:
  push:
    branches:
      - release

permissions:
  contents: read

concurrency:
  group: production-deployment
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: ${{ steps.deploy.outputs.url }}
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up pnpm
        uses: pnpm/action-setup@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate release
        run: pnpm check

      - name: Verify required Worker secrets
        run: |
          configured_secrets="$(pnpm wrangler secret list --format json)"
          for required_secret in PRIVATE_DATA_ENCRYPTION_KEY ADMIN_AUTH_SECRET; do
            if ! jq -e --arg name "$required_secret" 'map(.name) | index($name) != null' <<<"$configured_secrets" >/dev/null; then
              echo "Required Worker secret is not configured: $required_secret" >&2
              exit 1
            fi
          done

      - name: Apply production D1 migrations
        run: pnpm wrangler d1 migrations apply DB --remote

      - name: Verify no production migrations remain
        run: |
          migration_status="$(pnpm wrangler d1 migrations list DB --remote)"
          printf '%s\n' "$migration_status"
          grep -q "No migrations to apply" <<<"$migration_status"

      - name: Deploy Worker and frontend assets
        id: deploy
        run: |
          deploy_output="$(pnpm wrangler deploy 2>&1)"
          printf '%s\n' "$deploy_output"
          deploy_url="$(grep -Eo 'https://[A-Za-z0-9.-]+\.workers\.dev' <<<"$deploy_output" | tail -n 1)"
          if [[ -z "$deploy_url" ]]; then
            echo "Wrangler did not report a workers.dev production URL." >&2
            exit 1
          fi
          echo "url=$deploy_url" >> "$GITHUB_OUTPUT"

      - name: Run production smoke checks
        env:
          DEPLOY_URL: ${{ steps.deploy.outputs.url }}
        run: node scripts/smoke-production.mjs
